/*
Crie um programa que lê 6 valores inteiros e, em seguida, mostre na tela os valores lidos
na ordem inversa.
*/
#include <stdio.h>
int main(){
    int a[6];
    for(int i = 0; i < 6; i++)
        scanf("%d",&a[i]);
    return 0;
}
