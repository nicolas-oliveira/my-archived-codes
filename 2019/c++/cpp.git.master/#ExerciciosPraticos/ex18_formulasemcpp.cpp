// Criar as formulas matematicas em codigo c++
#include <iostream>
using namespace std;
int main(){
    int x, y , a, b, c, d, m, n;
    (x / y) + 1;
    (x + y)/(x - y);
    x + (y / a);
    b / (c + d);
    (a + b)(c / d);
    pow(pow(a + b,2),2);
    
    retun 0;
}